<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FreakLete"
             xmlns:services="clr-namespace:FreakLete.Services"
             x:Class="FreakLete.WorkoutPage"
             NavigationPage.HasNavigationBar="False"
             Title="Workout"
             BackgroundColor="{StaticResource Background}">

    <ContentPage.Resources>
        <local:DaysPerWeekFormatConverter x:Key="DaysPerWeekFmt" />
    </ContentPage.Resources>

    <Grid RowDefinitions="*,Auto">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Padding="20,16,20,24"
                                 Spacing="20">

                <!-- Page Header -->
                <VerticalStackLayout Spacing="4">
                    <Label x:Name="PageTitleLabel"
                           FontSize="28"
                           FontFamily="OpenSansSemibold"
                           TextColor="{StaticResource TextPrimary}"
                           Text="Programs" />
                    <Label x:Name="PageSubtitleLabel"
                           FontSize="13"
                           FontFamily="OpenSansRegular"
                           TextColor="{StaticResource TextSecondary}"
                           Text="Your training plans" />
                </VerticalStackLayout>

                <!-- Hero Card: Active Program -->
                <Border x:Name="HeroProgramCard"
                        StrokeShape="RoundRectangle 22"
                        StrokeThickness="1"
                        Padding="0"
                        IsVisible="False">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{StaticResource AccentSoft}" Offset="0.0" />
                            <GradientStop Color="{StaticResource Surface}" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Stroke>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{StaticResource Accent}" Offset="0.0" />
                            <GradientStop Color="{StaticResource AccentSoft}" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Stroke>
                    <VerticalStackLayout Padding="22,20" Spacing="12">
                        <Label x:Name="ActiveProgramBadge"
                               FontSize="11"
                               FontFamily="OpenSansSemibold"
                               TextColor="{StaticResource AccentGlow}"
                               Text="ACTIVE PROGRAM"
                               CharacterSpacing="1.5" />
                        <Label x:Name="HeroProgramName"
                               FontSize="24"
                               FontFamily="OpenSansSemibold"
                               TextColor="{StaticResource TextPrimary}"
                               LineBreakMode="WordWrap"
                               LineHeight="1.1" />
                        <Label x:Name="HeroProgramGoal"
                               FontSize="13"
                               FontFamily="OpenSansRegular"
                               TextColor="{StaticResource TextSecondary}" />

                        <!-- Stat pills row -->
                        <HorizontalStackLayout Spacing="8" Margin="0,2,0,0">
                            <Border x:Name="HeroFrequencyPill"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="{StaticResource SurfaceRaised}"
                                    Stroke="{StaticResource SurfaceBorder}"
                                    Padding="10,5">
                                <Label x:Name="HeroFrequencyLabel"
                                       FontSize="12"
                                       FontFamily="OpenSansSemibold"
                                       TextColor="{StaticResource TextPrimary}" />
                            </Border>
                            <Border x:Name="HeroDurationPill"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="{StaticResource SurfaceRaised}"
                                    Stroke="{StaticResource SurfaceBorder}"
                                    Padding="10,5"
                                    IsVisible="False">
                                <Label x:Name="HeroDurationLabel"
                                       FontSize="12"
                                       FontFamily="OpenSansSemibold"
                                       TextColor="{StaticResource TextPrimary}" />
                            </Border>
                            <Border x:Name="HeroWeeklyPill"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="{StaticResource AccentSoft}"
                                    Stroke="Transparent"
                                    Padding="10,5">
                                <Label x:Name="HeroWeeklyLabel"
                                       FontSize="12"
                                       FontFamily="OpenSansSemibold"
                                       TextColor="{StaticResource AccentGlow}" />
                            </Border>
                        </HorizontalStackLayout>

                        <!-- CTA -->
                        <Button x:Name="HeroStartBtn"
                                Text="Start Workout"
                                BackgroundColor="{StaticResource Accent}"
                                TextColor="White"
                                Padding="24,14"
                                CornerRadius="14"
                                FontSize="15"
                                FontFamily="OpenSansSemibold"
                                Clicked="OnHeroStartWorkoutClicked"
                                HorizontalOptions="FillAndExpand"
                                Margin="0,4,0,0" />
                    </VerticalStackLayout>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnHeroCardTapped" />
                    </Border.GestureRecognizers>
                </Border>

                <!-- Empty Hero: No Active Program -->
                <Border x:Name="EmptyHeroCard"
                        StrokeShape="RoundRectangle 22"
                        StrokeThickness="1"
                        Stroke="{StaticResource SurfaceBorder}"
                        Padding="0"
                        IsVisible="True">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{StaticResource SurfaceRaised}" Offset="0.0" />
                            <GradientStop Color="{StaticResource Surface}" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <VerticalStackLayout Padding="22,26" Spacing="12"
                                         HorizontalOptions="Center">
                        <Label x:Name="EmptyGetStartedBadge"
                               FontSize="11"
                               FontFamily="OpenSansSemibold"
                               TextColor="{StaticResource AccentGlow}"
                               Text="GET STARTED"
                               CharacterSpacing="1.5"
                               HorizontalTextAlignment="Center" />
                        <Label x:Name="EmptyNoActiveLabel"
                               FontSize="20"
                               FontFamily="OpenSansSemibold"
                               TextColor="{StaticResource TextPrimary}"
                               Text="No active program"
                               HorizontalTextAlignment="Center" />
                        <Label x:Name="EmptyDescLabel"
                               FontSize="13"
                               FontFamily="OpenSansRegular"
                               TextColor="{StaticResource TextSecondary}"
                               Text="Create a training program with FreakAI or start a quick workout"
                               HorizontalTextAlignment="Center"
                               LineBreakMode="WordWrap"
                               MaxLines="2" />
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,6,0,0">
                            <Button Grid.Column="0"
                                    Text="FreakAI"
                                    BackgroundColor="{StaticResource Accent}"
                                    TextColor="White"
                                    Padding="16,12"
                                    CornerRadius="14"
                                    FontSize="14"
                                    FontFamily="OpenSansSemibold"
                                    Clicked="OnGoToFreakAiClicked" />
                            <Button x:Name="EmptyQuickWorkoutBtn"
                                    Grid.Column="1"
                                    Text="Quick Workout"
                                    BackgroundColor="{StaticResource SurfaceStrong}"
                                    TextColor="{StaticResource TextPrimary}"
                                    Padding="16,12"
                                    CornerRadius="14"
                                    FontSize="14"
                                    FontFamily="OpenSansSemibold"
                                    Clicked="OnOpenNewWorkoutClicked" />
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Stats Row -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                    <Border Grid.Column="0"
                            StrokeShape="RoundRectangle 18"
                            Stroke="{StaticResource SurfaceBorder}"
                            StrokeThickness="1"
                            Padding="16,14">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="{StaticResource SurfaceRaised}" Offset="0.0" />
                                <GradientStop Color="{StaticResource Surface}" Offset="1.0" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                            <Label x:Name="ThisWeekBadge"
                                   FontSize="10"
                                   FontFamily="OpenSansSemibold"
                                   TextColor="{StaticResource TextSecondary}"
                                   CharacterSpacing="0.5"
                                   Text="THIS WEEK"
                                   HorizontalTextAlignment="Center" />
                            <Label x:Name="SessionsCountLabel"
                                   FontSize="26"
                                   FontFamily="OpenSansSemibold"
                                   TextColor="{StaticResource AccentGlow}"
                                   Text="0"
                                   HorizontalTextAlignment="Center" />
                            <Label x:Name="SessionsSubLabel"
                                   FontSize="10"
                                   FontFamily="OpenSansRegular"
                                   TextColor="{StaticResource TextSecondary}"
                                   Text="sessions"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Column="1"
                            StrokeShape="RoundRectangle 18"
                            StrokeThickness="1"
                            Padding="16,14">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="{StaticResource AccentSoft}" Offset="0.0" />
                                <GradientStop Color="{StaticResource Surface}" Offset="1.0" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Stroke>
                            <SolidColorBrush Color="{StaticResource Accent}" />
                        </Border.Stroke>
                        <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                            <Label x:Name="ProgramsBadge"
                                   FontSize="10"
                                   FontFamily="OpenSansSemibold"
                                   TextColor="{StaticResource AccentGlow}"
                                   CharacterSpacing="0.5"
                                   Text="PROGRAMS"
                                   HorizontalTextAlignment="Center" />
                            <Label x:Name="ProgramCountLabel"
                                   FontSize="26"
                                   FontFamily="OpenSansSemibold"
                                   TextColor="{StaticResource TextPrimary}"
                                   Text="0"
                                   HorizontalTextAlignment="Center" />
                            <Label x:Name="AvailableSubLabel"
                                   FontSize="10"
                                   FontFamily="OpenSansRegular"
                                   TextColor="{StaticResource TextSecondary}"
                                   Text="available"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <!-- Goal Filter Chips -->
                <ScrollView x:Name="GoalChipsScroll"
                            Orientation="Horizontal"
                            HorizontalScrollBarVisibility="Never"
                            IsVisible="False">
                    <HorizontalStackLayout x:Name="GoalChipsContainer"
                                           Spacing="8" />
                </ScrollView>

                <!-- Recommended Workouts Section -->
                <VerticalStackLayout x:Name="RecommendedSection"
                                     Spacing="12"
                                     IsVisible="False">
                    <Label x:Name="RecommendedLabel"
                           FontSize="18"
                           FontFamily="OpenSansSemibold"
                           TextColor="{StaticResource TextPrimary}"
                           Text="Recommended" />
                    <VerticalStackLayout x:Name="RecommendedCardsContainer"
                                         Spacing="10" />
                </VerticalStackLayout>

                <!-- Section Header -->
                <Label x:Name="AllProgramsLabel"
                       FontSize="18"
                       FontFamily="OpenSansSemibold"
                       TextColor="{StaticResource TextPrimary}"
                       Text="All Programs"
                       Margin="0,2,0,0" />

                <!-- Program Cards -->
                <CollectionView x:Name="ProgramsCollectionView"
                                SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical"
                                           ItemSpacing="10" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border StrokeShape="RoundRectangle 20"
                                    Stroke="{StaticResource SurfaceBorder}"
                                    StrokeThickness="1"
                                    Padding="0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="{StaticResource SurfaceRaised}" Offset="0.0" />
                                        <GradientStop Color="{StaticResource Surface}" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Grid RowDefinitions="Auto,Auto" Padding="0">
                                    <!-- Top: Program info -->
                                    <VerticalStackLayout Grid.Row="0"
                                                         Padding="18,16,18,12"
                                                         Spacing="8">
                                        <HorizontalStackLayout Spacing="10">
                                            <Label Text="{Binding Name}"
                                                   FontSize="16"
                                                   FontFamily="OpenSansSemibold"
                                                   TextColor="{StaticResource TextPrimary}"
                                                   LineBreakMode="TailTruncation"
                                                   HorizontalOptions="FillAndExpand" />
                                            <!-- Status pill -->
                                            <Border StrokeShape="RoundRectangle 10"
                                                    BackgroundColor="{StaticResource AccentSoft}"
                                                    Stroke="Transparent"
                                                    Padding="10,4"
                                                    VerticalOptions="Center">
                                                <Label Text="{Binding Status}"
                                                       FontSize="10"
                                                       FontFamily="OpenSansSemibold"
                                                       TextColor="{StaticResource AccentGlow}" />
                                            </Border>
                                        </HorizontalStackLayout>
                                        <Label Text="{Binding Goal}"
                                               FontSize="12"
                                               FontFamily="OpenSansRegular"
                                               TextColor="{StaticResource TextSecondary}"
                                               LineBreakMode="TailTruncation" />
                                    </VerticalStackLayout>

                                    <!-- Bottom: Metadata bar -->
                                    <Border Grid.Row="1"
                                            StrokeShape="RoundRectangle 0,0,20,20"
                                            BackgroundColor="{StaticResource Surface}"
                                            Stroke="Transparent"
                                            Padding="18,10">
                                        <Grid ColumnDefinitions="Auto,*,Auto">
                                            <Label Grid.Column="0"
                                                   Text="{Binding DaysPerWeek, Converter={StaticResource DaysPerWeekFmt}}"
                                                   FontSize="12"
                                                   FontFamily="OpenSansSemibold"
                                                   TextColor="{StaticResource TextSecondary}"
                                                   VerticalOptions="Center" />
                                            <Label Grid.Column="2"
                                                   Text="{x:Static services:AppLanguage.WorkoutViewDetails}"
                                                   FontSize="12"
                                                   FontFamily="OpenSansSemibold"
                                                   TextColor="{StaticResource AccentGlow}"
                                                   VerticalOptions="Center" />
                                        </Grid>
                                    </Border>
                                </Grid>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnProgramCardTapped" />
                                </Border.GestureRecognizers>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Empty State -->
                <Label x:Name="NoProgramsLabel"
                       Text="No training programs yet"
                       FontSize="14"
                       TextColor="{StaticResource TextSecondary}"
                       HorizontalTextAlignment="Center"
                       IsVisible="False"
                       Margin="0,8,0,0" />

                <!-- Quick Actions Row -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                    <Border Grid.Column="0"
                            StrokeShape="RoundRectangle 18"
                            Stroke="{StaticResource SurfaceBorder}"
                            StrokeThickness="1"
                            Padding="0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="{StaticResource SurfaceRaised}" Offset="0.0" />
                                <GradientStop Color="{StaticResource Surface}" Offset="1.0" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <VerticalStackLayout Padding="16,14" Spacing="8"
                                             VerticalOptions="Center"
                                             HorizontalOptions="Center">
                            <Border StrokeShape="RoundRectangle 12"
                                    BackgroundColor="{StaticResource AccentSoft}"
                                    Stroke="Transparent"
                                    WidthRequest="36"
                                    HeightRequest="36"
                                    HorizontalOptions="Center">
                                <Label FontSize="18"
                                       FontFamily="OpenSansSemibold"
                                       TextColor="{StaticResource AccentGlow}"
                                       Text="+"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center" />
                            </Border>
                            <Label x:Name="QuickAddLabel"
                                   FontSize="13"
                                   FontFamily="OpenSansSemibold"
                                   TextColor="{StaticResource TextPrimary}"
                                   Text="Quick Add"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnOpenNewWorkoutClicked" />
                        </Border.GestureRecognizers>
                    </Border>

                    <Border Grid.Column="1"
                            StrokeShape="RoundRectangle 18"
                            Stroke="{StaticResource SurfaceBorder}"
                            StrokeThickness="1"
                            Padding="0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="{StaticResource SurfaceRaised}" Offset="0.0" />
                                <GradientStop Color="{StaticResource Surface}" Offset="1.0" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <VerticalStackLayout Padding="16,14" Spacing="8"
                                             VerticalOptions="Center"
                                             HorizontalOptions="Center">
                            <Border StrokeShape="RoundRectangle 12"
                                    BackgroundColor="{StaticResource AccentSoft}"
                                    Stroke="Transparent"
                                    WidthRequest="36"
                                    HeightRequest="36"
                                    HorizontalOptions="Center">
                                <Image Source="calendar_icon.svg"
                                       WidthRequest="18"
                                       HeightRequest="18"
                                       HorizontalOptions="Center" />
                            </Border>
                            <Label x:Name="CalendarLabel"
                                   FontSize="13"
                                   FontFamily="OpenSansSemibold"
                                   TextColor="{StaticResource TextPrimary}"
                                   Text="Calendar"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnOpenCalendarPageClicked" />
                        </Border.GestureRecognizers>
                    </Border>
                </Grid>

            </VerticalStackLayout>
        </ScrollView>

        <local:BottomNavBar Grid.Row="1"
                            ActiveTab="workout" />
    </Grid>

</ContentPage>
